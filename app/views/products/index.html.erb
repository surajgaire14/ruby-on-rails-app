<h1>Products</h1>

<h2><%= t "hello" %></h2>

<%= link_to "Create New Product", new_product_path if authenticated? %>

<div id="products">
    <% @products.each do |product| %>
        <a href="/products/<%= product.id %>">
            <%= product.name %>
        </a>
    <% end %>
</div>
